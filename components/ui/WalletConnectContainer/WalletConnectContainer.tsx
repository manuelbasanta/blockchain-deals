"use client";

const WalletConnectContainer = ({
    children,
  }: {
    children: React.ReactNode;
  }) => {
    return (
        <>
            {children}
        </>
    );
}

export default WalletConnectContainer;